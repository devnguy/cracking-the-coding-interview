/**
 * Write a method to sort an array of strings so that all anagrams are next to
 * each other.
 */

// sort the string itself to put chars in order
// if sorted string == another string then group them together
// sort the strings then sort the array
